# Git Guidelines

## Safety Rules
- DO NOT run destructive commands like `git reset --hard`, `git clean -fd`, `git push --force` unless explicitly requested.
- Check git status before making large changes.

## Secrets & Exclusions
- DO NOT commit `.env`, `credentials`, `API keys`, `passwords`, `private keys`, large generated files, or build artifacts.
