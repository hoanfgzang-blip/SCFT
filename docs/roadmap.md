# Roadmap

## NOW
- Verify and audit current codebase.
- Implement explicit latency measurement for PC Screen (if 5-10ms target is strictly required).
- Unify the Android Mobile UI (React Native) with the Android Native app, or clarify their relationship.
- Improve error handling for USB connection loss during file transfers.

## NEXT
- Enhance Screen Copy (Android to PC) to use H.264 stream instead of slow PNG capture.
- Complete Transfer History implementation with real database (`.meta.json` indexing or SQLite).
- Add Pause/Resume functionality for File Transfer.

## LATER
- Data/Clipboard transfer between PC and Android.
- Audio streaming for PC Screen (if not yet fully implemented).
- Polish UX/UI on both PC and Mobile.
